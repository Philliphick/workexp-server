const express = require('express');
const appRoutes = express.Router();

// Define different end points for your application here
// Each end point will handle specific tasks based on the incoming HTTP requests

appRoutes.get('/', (req, res) => {
    // they will write a try catch block in here for example
});

appRoutes.get('/', (req, res) => {
    
});

module.exports = appRoutes;
